#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='Software Testing and QA',
      version='1.0.0',
      description='',
      packages=find_packages(exclude=['contrib', 'docs', 'test']),
      )
